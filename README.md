<!-- # Lógica de Programação com JavaScript

Este repositório reúne uma série de algoritmos desenvolvidos para o estudo de **estruturas de repetição** e **validação de dados**. O foco principal foi a implementação de fluxos de controle utilizando `while`, garantindo a integridade das informações inseridas pelo usuário.

---

##  Tecnologias e Conceitos

* **Linguagem:** JavaScript (ES6+)
* **Estruturas de Controle:** Laços de repetição `while` e condicionais `if/else`.
* **Tratamento de Dados:** Conversão de tipos (`Number`), tratamento de entradas vazias.

---

## Orientações das Tarefas

**Exercício 1:** Solicitar que o usuário digite um número positivo. Caso seja digitado um valor negativo, o programa deve continuar pedindo até que um valor válido seja inserido.

**Exercício 2:**: O usuário pode inserir notas (de 0 a 10) indefinidamente. O programa deve parar a leitura apenas quando um número negativo for digitado e, em seguida, exibir a média aritmética das notas válidas.

**Exercício 3:**: Solicitar valores de itens de compra de forma contínua. O programa deve encerrar a entrada de dados e exibir o valor total acumulado assim que o usuário digitar o valor `0`.

**Exercício 4:** Solicitar o primeiro nome e o sobrenome separadamente. O programa deve validar se os campos não estão vazios, repetindo a solicitação para cada campo até que ambos sejam preenchidos corretamente.

**Exercício 5:** Verificar a viabilidade de saques com base em um saldo inicial simulado. Caso o valor de saque solicitado seja maior que o saldo disponível, o programa deve pedir um novo valor. O ciclo se encerra quando o saldo atingir `0` ou um valor negativo.

--- -->

# Lógica de Programação com JavaScript

![JavaScript](https://img.shields.io/badge/logo-javascript-yellow?logo=javascript&logoColor=white) 
![HTML5](https://img.shields.io/badge/logo-html5-orange?logo=html5&logoColor=white)

Este repositório reúne uma série de algoritmos desenvolvidos para o estudo de **estruturas de repetição** e **validação de dados**. O foco principal foi a implementação de fluxos de controle utilizando `while`, garantindo a integridade das informações inseridas pelo usuário.

---

##  Tecnologias e Conceitos

* **Linguagem:** JavaScript (ES6+)
* **Estruturas de Controle:** Laços de repetição `while` e condicionais `if/else`.
* **Tratamento de Dados:** Conversão de tipos (`Number`), tratamento de entradas vazias.

---

## Orientações das Tarefas

**Exercício 1:** Solicitar que o usuário digite um número positivo. Caso seja digitado um valor negativo, o programa deve continuar pedindo até que um valor válido seja inserido.

**Exercício 2:** O usuário pode inserir notas (de 0 a 10) indefinidamente. O programa deve parar a leitura apenas quando um número negativo for digitado e, em seguida, exibir a média aritmética das notas válidas.

**Exercício 3:** Solicitar valores de itens de compra de forma contínua. O programa deve encerrar a entrada de dados e exibir o valor total acumulado assim que o usuário digitar o valor `0`.

**Exercício 4:** Solicitar o primeiro nome e o sobrenome separadamente. O programa deve validar se os campos não estão vazios, repetindo a solicitação para cada campo até que ambos sejam preenchidos corretamente.

**Exercício 5:** Verificar a viabilidade de saques com base em um saldo inicial simulado. Caso o valor de saque solicitado seja maior que o saldo disponível, o programa deve pedir um novo valor. O ciclo se encerra quando o saldo atingir `0` ou um valor negativo.

---